streamlit>=1.36
chromadb>=0.5
python-dotenv>=1.0
langdetect>=1.0.9
deep-translator>=1.11

# LLM providers (free) — you only strictly need the one you use,
# but installing both lets you switch via LLM_PROVIDER in .env
groq>=0.11
google-generativeai>=0.8
